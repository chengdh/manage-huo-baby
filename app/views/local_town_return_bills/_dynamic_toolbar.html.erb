<%= render :partial => "shared/carrying_bills/dynamic_toolbar",
  :locals => {:search_bill_path => 
              search_local_town_return_bills_path(
                                       :sort => "carrying_bills.bill_date desc,goods_no",
                                       :direction => "asc",
                                       :not_remote => true,
                                       :format => :js
                                      )
} %>
