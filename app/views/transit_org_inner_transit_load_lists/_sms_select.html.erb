<%= semantic_form_for resource,:url => {:action => :export_sms_txt,:format => :text},:method => :post,:html => {:id => "export_sms_form",:class => "bill_selector"} do |f|%>
<%end%>
<%= render "show" %>
