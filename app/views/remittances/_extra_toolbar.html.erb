<%if params[:action] == "show"%>
  <%if can? :validate,resource%>
    <li>
      <%= link_to image_tag("blue-folder-stamp.png",:alt => "审核确认") ,
        process_handle_remittance_path(resource,
                                       "update_attrs[validate_fee]" => 0,
                                       "update_attrs[validate_date]" => Time.now,
                                       "update_attrs[validate_user_id]" => current_user.id),
        :id => "btn_validate_remittance",
        :title => "审核确认",
        :class => "tipsy"
      %>
  </li>
  <%end%>
<%end%>
